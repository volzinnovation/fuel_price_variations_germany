# Standup Handoff (2026-03-06)

## Done Today
- Android app map provider migrated and stabilized on MapLibre.
- Map tiles switched to standard OpenStreetMap raster tiles (`tile.openstreetmap.org`).
- Android layout adjusted to respect system status bar/header area.
- Tankerkoenig offline apology handling is present in Android ViewModel messaging.
- App built and deployed successfully to attached USB device (`moto g86 5G`).
- Map marker interaction improved:
  - Map double-tap zoom disabled to avoid stealing marker double taps.
  - Marker double-tap window increased (700 ms).
  - Map center refresh callbacks limited to gesture-driven camera moves.

## Open Issues
- Android instrumentation UI test currently fails:
  - `:app:connectedDebugAndroidTest`
  - Failure: `No compose hierarchies found in the app` (Activity/Compose test startup issue).
- Need to confirm on-device UX that double-tapping a price marker always opens station detail across zoom levels.
- Android project is newly added and currently untracked in git history; ensure first commit scope is intentional.

## Suggested Next Session
1. Fix instrumentation bootstrap so Compose hierarchy is always available during tests.
2. Re-run `:app:connectedDebugAndroidTest` until green on real device.
3. Add/finish full UI test coverage for Diesel, E10, Favorites, Statistics, Info, manual coordinates, and offline Tankerkoenig states.
4. Verify map marker tap/double-tap behavior with screen recording on device and attach evidence to PR/notes.

## Commands Used Today
- `./gradlew :app:assembleDebug`
- `./gradlew :app:assembleDebugAndroidTest :app:testDebugUnitTest`
- `./gradlew :app:connectedDebugAndroidTest` (failing, see open issues)
- `adb install -r app/build/outputs/apk/debug/app-debug.apk`
- `adb shell monkey -p de.tankzeit.android -c android.intent.category.LAUNCHER 1`
